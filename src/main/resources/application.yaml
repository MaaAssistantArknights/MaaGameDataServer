spring:
  liquibase:
    enabled: true
    change-log: 'classpath:/db/changelog/liquibase.xml'
    default-schema: 'gamedata'
  application:
    name: maaGameDataService
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

server:
  port: 8080
